{
  "name": "moholocal-scripts",
  "version": "1.0.0",
  "type": "module",
  "description": "MoHoLocal processing scripts — run locally, not deployed",
  "scripts": {
    "process-signals": "node process-signals.mjs"
  },
  "dependencies": {
    "tesseract.js": "^5.1.1"
  }
}
