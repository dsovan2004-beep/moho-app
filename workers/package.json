{
  "name": "moho-ingestion-worker",
  "version": "1.0.0",
  "private": true,
  "description": "MoHoLocal weekly data ingestion — Cloudflare Worker with cron triggers",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.72.0"
  }
}
